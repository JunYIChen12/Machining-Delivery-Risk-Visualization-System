# 文档说明

当前目录包含：

- `latest-product-prototype.md`：最新产品原型文档的 Markdown 版，适合 GitHub 阅读、搜索和 diff。

## 关于 Word 原件

最新 Word 原件位于本地：

```text
D:/codex/MVP产品/产品原型—机加工行业.docx
```

当前 Codex GitHub 连接工具只能直接创建 UTF-8 文本文件，不能把 `.docx` 这类二进制文件直接提交到仓库路径。因此本次先上传 Markdown 版文档。

本地已经整理好完整项目包：

```text
D:/codex/MVP产品/github-package
D:/codex/MVP产品/machining-production-visibility.zip
```

其中包含 Word 原件和效果图。后续如果本机安装 `git` / `gh`，或提供可用的 GitHub token 给上传脚本，就可以把 Word 原件和图片也补传到仓库。
